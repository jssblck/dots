interface:
  display_name: "Merge Open PRs"
  short_description: "Stack, resolve, and merge the open PRs"
  default_prompt: "Use $merge-open-prs to snapshot the open PRs, assemble them into one native GitHub stack, resolve conflicts, and merge the stack in one operation."
